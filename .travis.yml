sudo: required
language: java
jdk:
  - openjdk8
  - openjdk9
  - openjdk11

script: mvn clean install

addons:
  chrome: stable

cache:
  directories:
    - node_modules

before_install:
  - chmod +x mvnw

after_success:
  - mvn clean test jacoco:report coveralls:report