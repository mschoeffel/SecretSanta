language: java
jdk:
  - openjdk8
  - openjdk9
  - openjdk11

addons:
  chrome: stable

script:
  - mvn clean install

cache:
  directories:
    - node_modules

before_install:
  - chmod +x mvnw

after_success:
  - mvn clean test jacoco:report coveralls:report